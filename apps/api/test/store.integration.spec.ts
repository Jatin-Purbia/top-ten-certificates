import { beforeEach, describe, expect, it } from 'vitest';
import { Store } from '../src/store.js';
describe('demo repository integration',()=>{let store:Store;beforeEach(async()=>{process.env.DEMO_MODE='true';store=new Store();await store.onModuleInit()});it('hashes claims and never returns their hash',async()=>{const draft=(await store.listCycles()).find(c=>c.status==='draft')!;const created=await store.createCandidate(draft.id,{participantId:'TEST-001',certificateNumber:'T-001',nameHindi:'परीक्षण',nameEnglish:'Test Candidate',guardianName:'Demo Guardian',className:'5',age:10,city:'जयपुर',score:99,rank:1,resultDate:'2026-08-04',photoPath:null});expect(created.claimCode.length).toBeGreaterThan(10);expect(await store.verifyClaim(draft.id,'TEST-001',created.claimCode)).toBeTruthy();expect(JSON.stringify(created.candidate)).not.toContain('claimHash')});it('rejects an atomic import with duplicate ranks',async()=>{const draft=(await store.listCycles()).find(c=>c.status==='draft')!;const row={participantId:'TEST-A',certificateNumber:'A',nameHindi:'अ',nameEnglish:'A',guardianName:'G',className:'5',age:10,city:'City',score:1,rank:1,resultDate:'2026-08-04',photoPath:null};await expect(store.importCandidates(draft.id,[row,{...row,participantId:'TEST-B',certificateNumber:'B'}])).rejects.toThrow('DUPLICATE_RANK');expect(await store.listCandidates(draft.id)).toHaveLength(0)});it('purge is idempotent',async()=>{const runs=await store.purgeExpired();expect(runs.some(r=>r.status==='succeeded')).toBe(true);expect(await store.purgeExpired()).toHaveLength(0)});it('applies settings only through the explicit active-cycle operation',async()=>{const published=(await store.listCycles()).find(c=>c.status==='published')!,old=published.expiresAt;await store.setSettings(7,'demo-super-admin');expect((await store.getCycle(published.id))!.expiresAt).toBe(old);await store.applySettings(7,'demo-super-admin');expect((await store.getCycle(published.id))!.downloadWindowDays).toBe(7)});});
