'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { createClient } from '@supabase/supabase-js';
import { Button, Card, Field } from '@pathey/ui';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),router=useRouter();const demo=(role:string)=>{localStorage.setItem('demo_admin',role);router.push('/admin')};const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setError('Supabase browser configuration is missing. Use a demo role for local development.');return}const{data,error}=await createClient(url,key).auth.signInWithPassword({email,password});if(error||!data.session){setError(error?.message??'Sign-in failed');return}localStorage.setItem('admin_token',data.session.access_token);router.push('/admin')};return <main className="public-page"><Card style={{width:'min(460px,100%)'}}><div className="brand" style={{color:'var(--navy)'}}><span className="brand-mark"/>पाथेय कण</div><h1>Administrator sign in</h1><p style={{color:'var(--muted)'}}>Use your Supabase administrator account.</p>{error&&<p className="notice notice-danger" role="alert">{error}</p>}<form onSubmit={submit} style={{display:'grid',gap:16}}><Field label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><Field label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><Button type="submit">Sign in</Button></form>{process.env.NODE_ENV!=='production'&&<div style={{marginTop:24,borderTop:'1px solid var(--line)',paddingTop:20}}><strong>Local demo access</strong><div className="actions" style={{marginTop:10}}><Button variant="secondary" onClick={()=>demo('demo-super-admin')}>Super Admin</Button><Button variant="secondary" onClick={()=>demo('demo-certificate-admin')}>Certificate Admin</Button><Button variant="secondary" onClick={()=>demo('demo-viewer')}>Viewer</Button></div></div>}</Card></main>}
