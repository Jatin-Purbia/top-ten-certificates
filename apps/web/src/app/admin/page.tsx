'use client';
import { useQuery } from '@tanstack/react-query';
import Link from 'next/link';
import { Badge, Card } from '@pathey/ui';
import { adminFetch, formatIndia } from '@/lib/api';
export default function Dashboard(){const{data,isPending,error}=useQuery({queryKey:['dashboard'],queryFn:()=>adminFetch<any>('/admin/dashboard')});if(isPending)return <p>Loading dashboard…</p>;if(error)return <p className="notice notice-danger">{error.message}</p>;if(!data?.data)return <p className="notice notice-danger">Dashboard data is unavailable.</p>;const d=data.data;return <><div className="page-head"><div><h1>Operations dashboard</h1><p>Published results, certificate uptake, and retention health.</p></div>{d.currentCycle&&<Link className="btn btn-primary" href={`/admin/cycles/${d.currentCycle.id}`}>Open current cycle</Link>}</div><div className="metrics"><Card className="metric"><div className="metric-label">Current candidates</div><div className="metric-value">{d.candidateCount}</div></Card><Card className="metric"><div className="metric-label">Downloaded</div><div className="metric-value">{d.downloaded}</div></Card><Card className="metric"><div className="metric-label">Not downloaded</div><div className="metric-value">{d.notDownloaded}</div></Card><Card className="metric"><div className="metric-label">Nearing expiry</div><div className="metric-value">{d.nearingExpiry}</div></Card></div><div className="grid-2"><Card><h2>Cycle timeline</h2>{d.currentCycle?<div><p><Badge tone="success">Published</Badge></p><h3>{d.currentCycle.title}</h3><p>Certificate deadline: <strong>{formatIndia(d.currentCycle.expiresAt)}</strong></p></div>:<p className="empty">No result is currently published.</p>}{d.nextCycle&&<div style={{borderTop:'1px solid var(--line)',marginTop:18,paddingTop:18}}><p><Badge tone="warning">Up next</Badge></p><strong>{d.nextCycle.title}</strong><p>{formatIndia(d.nextCycle.publicationAt)}</p></div>}</Card><Card><h2>Retention & cleanup</h2>{d.lastCleanup?<><p><Badge tone={d.lastCleanup.status==='succeeded'?'success':'danger'}>{d.lastCleanup.status}</Badge></p><p>Last run: {formatIndia(d.lastCleanup.startedAt)}</p><p>{d.lastCleanup.deletedRecords} personal records removed</p></>:<p>No cleanup run recorded yet.</p>}<p>Next expected: <strong>{formatIndia(d.nextExpectedCleanup)}</strong></p></Card></div><Card style={{marginTop:18}}><h2>Recent administrative activity</h2>{d.recentActivity.length?<div className="table-wrap"><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th></tr></thead><tbody>{d.recentActivity.map((a:any)=><tr key={a.id}><td>{formatIndia(a.createdAt)}</td><td>{a.action}</td><td>{a.entityType}</td></tr>)}</tbody></table></div>:<p className="empty">Activity will appear as administrators make changes.</p>}</Card></>}
